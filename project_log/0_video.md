# 视频录制脚本 — 逐镜指引

> 视频 = 5 分, MP4 格式, ≤500MB, 命名 `v_Tue34_w_10_rv_FanXiaole_ChenJunxi_LiuYijun.mp4`
> 上传云盘 (不打包进 zip), 可剪辑, 不可使用 AI 语音
> **AI 工具声明：** 本项目使用 Claude Code (代码/文档辅助) 和 Gemini (可视化页面 HTML 设计)

---

## 评分映射

| # | 要求 | 分值 | 对应镜头 | 建议时长 |
|---|------|------|---------|---------|
| 1 | 全员出镜，介绍姓名学号和分工 | 0.5 | 镜头 1 | ~30s |
| 2 | 展示电脑界面、FPGA、外设接线 | 1.5 | 镜头 2 | ~40s |
| 3 | ≥2 个复杂用例完整验证 | 3 | 镜头 3, 4 | ~2min |
| 4 | Bonus 工具介绍 + 功能演示 | 与上合并 | 镜头 5–10 | ~5min |

**建议总时长: 7–8 分钟**

---

## 镜头 1: 全员介绍 (~30s) — 要求 #1

**画面:** 三人并排面对镜头。画面下方叠字幕: 姓名 + 学号 + 分工。

**口播 (范晓乐):**
> "我们是第 Tue34_w_10 组。我是范晓乐，学号 12412307，负责 Verilog 硬件设计、VGA 控制器和五级流水线实现。"

**口播 (陈俊希):**
> "我是陈俊希，学号 12411025，负责 Vivado 综合实现、EGO1 上板测试和 Difftest 差分测试验证。"

**口播 (刘一骏):**
> "我是刘一骏，学号 12411922，负责 RISC-V 汇编代码编写、贪吃蛇游戏开发和 RARS 模拟验证。"

**口播 (范晓乐):**
> "本项目使用了两个 AI 辅助工具：Claude Code 用于代码审查和文档优化，Gemini 用于可视化页面 HTML 设计。所有 AI 生成内容均经人工审查和验证修正。"

---

## 镜头 2: 硬件展示 (~40s) — 要求 #2

**画面:** 全景俯拍桌面 → 推近各组件特写。

**旁白:**
> "这是我们使用的 EGO1 开发板，搭载 XC7A35T FPGA。Micro-USB 连接学生机供电和烧录。VGA 口通过 VGA 线连接外接显示器。板上 5 个按键用于贪吃蛇控制。右 8 拨码开关最高位 SwitchIn[15] 用于切换单周期和流水线模式。"

**特写清单 (逐一推近):**
1. EGO1 全景 (板子型号 XC7A35T 可见)
2. Micro-USB → 学生机
3. VGA 线 → 显示器
4. 5 个按键位置 (btn[0]–btn[4])
5. 右 8 拨码开关 (特写 SwitchIn[15])
6. Vivado 工程界面 (Sources 面板显示 16 个 .v 文件 + 2 个 .txt 文件)

---

## 镜头 3: 基础功能 — Difftest 33/33 PASS (~60s) — 要求 #3 (用例1)

**画面:** Vivado 综合完成 → 烧录 TopDebug.bit → 串口 Python 脚本运行。

**旁白:**
> "首先验证基础 CPU 功能。SwitchIn[15] 拨下，单周期模式。通过 UART 串口运行 difftest 差分测试框架，10 个测试用例共 33 组数据自动比对。"

**画面:** 终端输出定格特写。

**旁白:**
> "33 组全部通过。覆盖 AND、SLL、SRA、LUI+ADD、JAL+AUIPC、JAL+JALR、Fibonacci、Popcount、IEEE754 浮点分类、Q3.4 量化全部 10 个 Case。"

**关键展示:**
- Vivado 烧录流程 (加速播放)
- 终端显示 "33/33 passed" 特写
- SwitchIn[15] 拨下确认单周期模式

---

## 镜头 4: 手动验证 2 个复杂用例 (~60s) — 要求 #3 (用例2&3)

**旁白:**
> "接下来手动验证两个复杂用例。"

**Case 6 — Fibonacci:**
> "拨码开关设为 Case 6，操作数 n=3。CPU 执行后 LED 显示 0x02，即第 3 个斐波那契数为 2。再测试 n=1→1, n=2→1, n=4→3，全部正确。"

**Case 8 — IEEE754 浮点分类:**
> "拨码开关切到 Case 8。输入 16-bit 半精度值 0x7C00 正无穷大 → LED 输出类型码 1。0xFC01 NaN→输出 2。0x2026 规约正数→输出 3。0x0003 非规约正数→输出 4。0x8000 负零→输出 0。覆盖全部 5 种浮点类型，9 组数据全部正确。"

**关键展示:** LED 显示实时结果，与预期值逐项比对。

---

## 镜头 5: Bonus — VGA 文本显示 (~60s) — Bonus ①

**画面:** 显示器从 "No Signal" → 出现彩色字符。

**旁白:**
> "第一个 Bonus——VGA 文本显示控制器。通过 UART 串口在地址 0xFFFF_0100 写入亮绿色 A、亮红色 B、白色感叹号三个测试字符。CPU 通过 MMIO 写入帧缓冲，VGA 控制器以 25MHz 独立时钟扫描显示。"

**画面:** 切到 VGA.v 代码特写。

**旁白:**
> "VGA.v 包含 VGA 时序生成——640×480@60Hz、128 字符 8×16 字模 ROM、2400 字帧缓冲和 2 级像素着色流水线。完全用 Verilog 手写，不使用任何 IP 核。"

**画面:** 切到 DataMemory.v 帧缓冲代码。

**旁白:**
> "帧缓冲是 FPGA 硬核双端口 BRAM——CPU 侧 12.5MHz 写入，VGA 侧 25MHz 读出。两个系统完全异步，通过 BRAM 原生双端口实现零开销的软硬件通信。这就是 MMIO 的核心思想：CPU 只做普通的 sw 指令写内存，但硬件译码器把这次写操作路由到了 VGA 帧缓冲。"

**关键展示:**
- 显示器从黑屏 → 出现彩色字符
- VGA.v 代码结构特写 (字模 ROM、时序生成、像素着色)
- DataMemory.v 双端口 BRAM 代码特写

---

## 镜头 6: Bonus — 贪吃蛇游戏 (~90s) — Bonus ②

**画面:** 显示器出现贪吃蛇游戏画面 → 操作按键。

**旁白:**
> "贪吃蛇游戏——纯 RISC-V RV32I 汇编实现，416 条指令。没有操作系统、没有库函数，直接运行在自研 CPU 上。按键 btn[0] 上、btn[1] 下、btn[2] 左、btn[3] 右控制方向。"

**画面:** 蛇正常移动约 10 秒。

**画面:** 蛇吃到红色星号食物 → 蛇身变长。

**旁白:**
> "食物坐标由 16-bit 线性反馈移位寄存器 LFSR 伪随机生成。蛇身存储在 512 元素的环形缓冲区中，使用 HEAD 和 TAIL 索引追踪。"

**画面:** 故意撞墙 → 屏幕显示 "GAME OVER"。

**旁白:**
> "撞墙或撞到自己游戏结束。按 btn[4] 重新开始。每帧只重绘 3 个变化位置——新蛇头、旧蛇头变身体、擦除蛇尾——不清全屏，这是增量渲染的核心优化。"

**关键展示:**
- 蛇正常移动 ≥10s 连续操作
- 吃食物、蛇身增长
- 撞墙 Game Over → btn[4] 重启
- snake.asm 环形缓冲区代码特写

---

## 镜头 7: Bonus — ISA 硬件加速指令 (~60s) — Bonus ③

**画面:** ALU.v 代码特写 → popcount/clz/ctz 组合逻辑 function。

**旁白:**
> "ISA 扩展实现了三条硬件加速指令：POPCNT 统计 1 的个数、CLZ 统计前导零、CTZ 统计尾部零。全部纯组合逻辑，单周期完成。"

**画面:** Decoder.v custom_op 译码特写。

**旁白:**
> "使用 funct7 第 0 位区分自定义指令——所有标准 RV32I R-type 指令的这一位都是 0，自定义设为 1，实现零开销区分。"

**画面:** 终端显示 18/18 测试 PASS。

**旁白:**
> "18 组边界值测试全部通过。以 POPCNT 为例——同样的 32 位 popcount 计算，软件逐位统计需要约 100 条指令、100 个周期；硬件一条指令 1 个周期完成，加速比约 100 倍。这就是硬件加速的意义。"

**关键展示:**
- ALU.v popcount 分治法 5 级加法树代码
- Decoder.v custom_op 区分机制
- 测试结果 18/18 PASS
- 软件 vs 硬件对比

---

## 镜头 8: Bonus — 五级流水线 + 模式切换 (~90s) — Bonus ④

**画面:** SwitchIn[15] 拨下 → 拨上。

**旁白:**
> "五级流水线 CPU。SwitchIn[15] 拨下是单周期模式，拨上切换为流水线模式。两个 CPU 在 TopDebug.v 中同时实例化，共享同一套外设和 Debug 接口。"

**画面:** CPUTopPipeline.v 代码结构特写。

**旁白:**
> "流水线包含完整的冒险处理。数据转发：EX/MEM 和 MEM/WB 阶段的 ALU 结果直接转发到 EX 阶段的 ALU 输入端，大部分数据冒险零延迟解决。"

**画面:** HazardUnit.v 转发逻辑特写。

**旁白:**
> "Load-Use 冒险：lw 指令后紧跟使用加载值的指令时，硬件自动暂停 1 个周期并插入 NOP 气泡。控制冒险：分支指令采用预测不跳转策略，分支成立时清除 IF/ID 流水线寄存器。"

**画面:** 串口 Debug 读寄存器 → 拨开关 → 再次读寄存器。

**旁白:**
> "Debug 控制器在两种模式下都能暂停 CPU 并观察寄存器值。切换模式不需要重新烧录——拨一下开关即可。"

**关键展示:**
- SwitchIn[15] 拨动 → 模式切换
- Pipeline 冒险处理代码特写
- 串口 Debug 两种模式读寄存器对比

---

## 镜头 9: Bonus — 可视化工具 (~30s) — Bonus ⑤

**画面:** 浏览器打开 visualizer.html → 切换指令 → 数据通路高亮。

**旁白:**
> "CPU 数据通路可视化工具。纯 HTML 单文件，双击即用。支持 12 条 RV32I 指令的逐组件逐路径动画展示。5 个流水线阶段着色，7 条控制信号实时显示。不需要上网、不需要安装，适合教学使用。"

**关键展示:**
- visualizer.html 界面全貌
- 切换 2-3 条不同指令展示通路变化
- 控制信号表实时更新

---

## 镜头 10: 软件乘法 (~20s) — 溢出展示

**画面:** soft_mul.asm 算法注释特写 + 测试结果。

**旁白:**
> "最后展示软硬件协同的另一个例子——纯汇编移位相加乘法。同样计算 3×5，软件需要约 200 个周期逐位检查乘数、累加被乘数；而硬件乘法器只需 1 条指令 1 个周期。直观体现硬件加速的价值。"

---

## 录制前检查清单

### 硬件
- [ ] EGO1 板子正常 (上电 LED 亮)
- [ ] VGA 显示器 + VGA 线完好
- [ ] Micro-USB 线 (烧录 + 串口通信正常)
- [ ] 5 个按键正常、16 个拨码开关正常
- [ ] 学生机 Vivado 2017.4 可综合

### 软件
- [ ] Vivado 工程可正常综合 (16 个 .v + 2 个 .txt)
- [ ] `batch_test.txt` 在 assembly/ 和 source/new/ 下
- [ ] difftest Python 脚本可正常运行 → 33/33 PASS
- [ ] SwitchIn[15]=0 确认单周期模式为默认
- [ ] VGA 测试: 串口写帧缓冲 → 显示器显示彩色字符
- [ ] Snake: 可正常游玩 (移动/吃食物/撞墙/重启)
- [ ] ISA 测试: 18 组 PASS
- [ ] Pipeline: SwitchIn[15]=1 切换正常，Debug 可读寄存器
- [ ] Visualizer: visualizer.html 可双击打开

### 环境
- [ ] 三人均在现场
- [ ] 桌面整洁、光线充足
- [ ] 录制设备支架固定、画面稳定
- [ ] 录制前完整走一遍流程

### 提交
- [ ] 文件名: `v_Tue34_w_10_rv_FanXiaole_ChenJunxi_LiuYijun.mp4`
- [ ] 格式: MP4, ≤500MB
- [ ] 无 AI 生成语音
- [ ] 上传云盘 (不打进压缩包)
